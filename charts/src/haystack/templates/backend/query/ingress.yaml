{{- if .Values.backend.query.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "haystack.backend.query.fullname" . }}
  labels:
    {{- include "haystack.backend.queryLabels" . | nindent 4 }}
  {{- with .Values.backend.query.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.backend.query.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.backend.query.ingress.tls }}
  tls:
    {{- range .Values.backend.query.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.backend.query.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "haystack.backend.query.fullname" $ }}
                port:
                  number: {{ $.Values.backend.query.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
