{{- if .Values.backend.query.servicemonitor.enabled }}
{{- if (lookup "monitoring.coreos.com/v1" "CustomResourceDefinition" "" "servicemonitors.monitoring.coreos.com") }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "haystack.backend.query.fullname" . }}
  labels:
    {{- include "haystack.backend.queryLabels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "haystack.backend.queryLabels" . | nindent 4 }}
  endpoints:
    - port: http
      interval: 30s
{{- end }}
{{- end }}